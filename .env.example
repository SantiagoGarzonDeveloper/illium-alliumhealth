# Copia este archivo como ".env" y rellena los valores reales.
# El ".env" NO se sube al repositorio (está en .gitignore).

# Clave de la IA (Groq) — necesaria para el chatbot, el Quiz y los protocolos.
# Pídesela a Santiago, o crea una gratis en https://console.groq.com/keys
VITE_GROQ_API_KEY=
